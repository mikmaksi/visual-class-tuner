[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "visual_class_tuner"
version = "0.0.1"
requires-python = ">=3.9"
dynamic = ["dependencies"]

[tool.setuptools.packages]
find = {}

[tool.setuptools.dynamic]
dependencies = {file = "requirements.txt"}
